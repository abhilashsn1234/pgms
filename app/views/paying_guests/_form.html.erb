<%= form_with(model: paying_guest) do |form| %>
  <% if paying_guest.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(paying_guest.errors.count, "error") %> prohibited this paying_guest from being saved:</h2>

      <ul>
        <% paying_guest.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :paying_guest, style: "display: block" %>
    <%= form.text_field :paying_guest %>
  </div>

  <div>
    <%= form.label :address, style: "display: block" %>
    <%= form.text_area :address %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
